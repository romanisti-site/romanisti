<section class="statement">
  <div class="container">
    <p class="eyebrow">The Institution</p>
    <blockquote>
      Romanisti exists where city and club become one.
    </blockquote>
    <p>
      It interprets AS Roma through the enduring character of Rome, and Rome
      through the living story of its football club.
    </p>
    <a href="/about/">About Romanisti</a>
  </div>
</section>

<style>
  .statement {
    border-block: 1px solid var(--color-line);
    padding-block: clamp(4rem, 9vw, 8rem);
  }

  blockquote {
    font-family: var(--font-display);
    font-size: clamp(2.3rem, 1.5rem + 4vw, 6rem);
    font-weight: 500;
    letter-spacing: -0.045em;
    line-height: 0.98;
    margin: var(--space-3) 0;
    max-width: 62rem;
  }

  .statement p:not(.eyebrow) {
    color: var(--color-muted);
    font-size: 1.2rem;
    line-height: 1.6;
    max-width: 44rem;
  }

  a {
    display: inline-block;
    font-family: var(--font-sans);
    font-size: 0.82rem;
    margin-top: var(--space-3);
  }
</style>
