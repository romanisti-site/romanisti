---
// RC12.1 route safeguard: the Works Room is now the primary architectural
// entrance for observations. Preserve the archive and individual observation
// routes, while sending legacy /observations/ links to /works/.
return Astro.redirect('/works/', 302);
---
